name: Build pbuilder chroots
on:
  - push
  - workflow_dispatch

permissions:
  contents: write

concurrency:
  group: build
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  metadata:
    name: Gather build metadata
    runs-on: ubuntu-22.04

    outputs:
      baseurl: ${{ steps.pbuilder-chroots.outputs.baseurl }}
      build_ids: ${{ steps.matrix.outputs.build_ids }}
      sources: ${{ steps.matrix.outputs.sources }}

    steps:
      - name: Check out code
        uses: actions/checkout@v3
        with:
          path: src

      - name: Check out repositories
        uses: actions/checkout@v3
        with:
          ref: repos
          path: repos

      - name: Install dependencies
        run: sudo apt install -y grep-dctrl

      - name: Detect available pbuilder-chroots
        id: pbuilder-chroots
        run: |
          baseurl="$(./src/.scripts/pbuilder-chroots_baseurl.sh)"
          echo "baseurl=${baseurl}" >> $GITHUB_OUTPUT
          echo "baseurl=${baseurl}" >> $GITHUB_ENV

      - name: Generate build matrixes
        id: matrix
        run: |
          metadata="$(./src/.scripts/metadata.sh ./src ./repos "${baseurl}")"

          sources="$(echo "${metadata}" | jq -cM '{"source": .source}')"
          echo "sources=${sources}" >> $GITHUB_OUTPUT

          build_ids="$(echo "${metadata}" | jq -cM '{"build_id": .build_id}')"
          echo "build_ids=${build_ids}" >> $GITHUB_OUTPUT

  sources:
    name: "Sources"
    runs-on: ubuntu-22.04
    needs:
      - metadata

    strategy:
      matrix: ${{ fromJSON(needs.metadata.outputs.sources) }}

    steps:
      - name: Split metadata
        id: metadata
        run: |
          echo "repo=$(echo ${{ matrix.source }} | cut -d' ' -f1)" >> $GITHUB_OUTPUT
          echo "gh_fullrepo=$(echo ${{ matrix.source }} | cut -d' ' -f2)" >> $GITHUB_OUTPUT
          echo "gh_repo=$(echo ${{ matrix.source }} | cut -d' ' -f2 | cut -d/ -f2)" >> $GITHUB_OUTPUT
          echo "ref=$(echo ${{ matrix.source }} | cut -d' ' -f3)" >> $GITHUB_OUTPUT

      - name: Check out code
        uses: actions/checkout@v3
        with:
          path: src

      - name: Check out source repository
        uses: actions/checkout@v3
        with:
          repository: ${{ steps.metadata.outputs.gh_fullrepo }}
          ref: ${{ steps.metadata.outputs.ref }}
          path: ${{ steps.metadata.outputs.gh_repo }}
          fetch-depth: 0

  build:
    name: "Build"
    runs-on: ubuntu-22.04
    needs:
      - metadata
      - sources

    strategy:
      matrix: ${{ fromJSON(needs.metadata.outputs.build_ids) }}

    steps:
      - name: Debug
        run: echo ${{ matrix.build_id }}
